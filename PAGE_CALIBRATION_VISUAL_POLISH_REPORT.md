# 页码校准页面视觉精修报告

## 完成情况

本轮只调整“页码校准”页面及侧栏的视觉与前端交互，没有修改页码映射算法、数据库结构、全文搜索、PDF 解析或导入流程。现有搜索、状态筛选、拼音排序、右侧详情、自动检测和安全移除功能均保留。

正式桌面包：`G:\ME_Finder\dist\MEFinder\文献原句定位器.exe`

## 刷新图标异常

原按钮并非字体图标，但旧 inline SVG 只包含一段不完整圆弧和一条折线，缺少另一侧回转箭头，因此轮廓视觉上接近字母 C。问题来自 SVG path 本身，不是 Windows 字体或字符编码。

现已改为完整的 `refresh-cw` inline SVG：

- `viewBox="0 0 24 24"`
- 18×18px 可见尺寸
- `fill="none"`
- `stroke="currentColor"`
- `stroke-width="1.8"`
- round linecap / linejoin
- 两段圆弧和两组箭头折线

按钮为 44×44px、12px 圆角、细边框和浅色背景，包含 `title="刷新文献列表"` 与同名 `aria-label`。刷新时按钮禁用，SVG 以 760ms 线性周期平滑旋转，完成后停止并恢复可点击状态。

刷新会保存列表滚动位置，且不改变搜索词、状态筛选、排序字段、排序方向和当前详情文献；刷新过程中不再用骨架屏替换整个列表。

## 修改文件

- `src/me_finder/web.py`
  - 完整刷新 SVG、旋转状态和防重复刷新。
  - 可点击统计胶囊、工具栏对齐、自定义 select 箭头。
  - 卡片信息层级、颜色、间距、状态动画和完整更多菜单。
  - 侧栏选中态、颜色变量和响应式细节。
  - 支持 `?page=calibration` 本地只读深链接，用于真实页面截图验收。
- `src/me_finder/calibration_library.py`
  - 映射摘要调整为“PDF 第 X 页 → 引用第 X 页”，并单独返回映射区间数量。
- `tests/test_calibration_library_ui.py`
  - 增加刷新 SVG、动画、滚动保持、统计筛选、卡片层级与更多菜单测试。

## 顶部与工具栏

页码校准标题为 29px，副标题为 14px，间距 6px。统计项改为紧凑按钮：

- PDF 总数
- 已校准
- 待校准
- 待确认
- 检测失败

点击任一统计胶囊会同步切换下方状态筛选；当前筛选使用浅蓝背景和细描边，其余保持中性灰。

搜索框与刷新按钮同为 44px 高，间距 11px。第二行左侧为状态筛选，右侧为排序字段和方向。排序 select 为 38px 高、10px 圆角，并使用独立 inline SVG 下拉箭头，不依赖系统字体符号。

## 卡片层级

卡片采用 16px 圆角、1px 浅边框、18–19px padding 和极轻阴影。hover 上移约 1.5px，过渡 180ms。

信息层级调整为：

1. 中性 PDF/MinerU 来源标签、彩色状态标签、更多菜单。
2. 17px 书名和 13px 作者。
3. 页数与文件大小。
4. “PDF 第 X 页 → 引用第 X 页”主摘要。
5. “共 X 个映射区间”次要摘要。
6. “自动映射 · 高可信”或“人工映射”。
7. 状态相关操作和导入日期。

来源标签始终使用中性灰；只有校准状态使用绿、橙、红或蓝。映射方法采用普通文字，不再和状态挤在同一个彩色标签中。作者缺失统一显示“作者信息待完善”。

检测失败卡片只显示一次红色“未找到可靠页码序列”，不重复堆叠。

已校准卡片的“查看映射”使用中性 ghost button；待校准、待确认和检测失败操作才使用蓝色文字。卡片主体仍可点击进入详情。

## 更多菜单

右上角 `···` 打开 180px 自定义浮层，包含：

- 打开原文
- 查看映射
- 自动检测页码
- 编辑书目信息
- 从文献库移除

菜单为 12px 圆角、38px 行高和轻微阴影，点击外部关闭。“从文献库移除”在分隔线下方使用红色文字，不与日常操作相邻。

## 颜色与间距

采用以下基础色：

- 页面：`#F6F7F9`
- 侧栏：`#F1F2F5`
- 卡片：`#FFFFFF`
- 主文字：`#181A1F`
- 次要文字：`#717680`
- 弱文字：`#A0A5AE`
- 分隔线：`#E7E9ED`
- 强调色：`#0A84FF`
- 成功：`#2DA44E`
- 警告：`#D97706`
- 错误：`#DC3C3C`

没有使用渐变背景、玻璃拟态或强投影。

## 状态更新

自动检测开始时，仅对应卡片显示带轻量 spinner 的“正在检测”。在“全部”筛选且不是按状态排序时，只替换当前卡片 DOM；其他卡片不会闪烁或重新排列。若当前筛选或排序确实依赖状态，则按逻辑重新计算列表。

## 响应式验收

使用正式应用后端和当前 11 本真实 PDF，通过本机 Edge headless 截取并检查：

- 1680×950：三列卡片。
- 1000×820：两列卡片。
- 800×820：单列卡片，统计胶囊、筛选和排序正常换行。

验收截图：

- `build/page-calibration-polished-final.png`
- `build/page-calibration-polished-medium.png`
- `build/page-calibration-polished-narrow.png`

截图中未发现文字重叠、按钮错位、卡片压缩或刷新箭头缺失。

## 测试结果

- 全套 60 项自动测试通过。
- HTML 内嵌 JavaScript 通过 Node `--check`。
- 正式 Windows exe 打包成功。
- 真实运行库统计保持：PDF 11 本，已校准 10 本，检测失败 1 本。
- 原有 Word/PDF 搜索、页码映射、自动检测、筛选、拼音排序和删除服务回归测试全部通过。

## 已知限制

- 极长且缺少人工书目标题的历史文件仍会回退显示原文件名；卡片最多显示两行并省略，不会破坏布局。
- pywebview 窗口本身不支持当前 Windows Graphics Capture 接口，因此视觉验证使用正式应用的本地后端页面和同机 Edge 渲染；页面内容、CSS、JavaScript 和真实 API 数据与桌面窗口相同。
