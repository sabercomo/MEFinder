# 原书页码获取策略

审计日期：2026-07-05

## 基本原则

本项目需要返回“原书页码”。不能用 PDF 页面序号、DOCX 段落序号、字符偏移、检索结果序号或数据库行号冒充原书页码。

原书页码必须来自可验证的页码来源，例如：

- 书籍正文或页脚中印刷出来的页码；
- Word 排版字段 `PAGE` 在受控排版环境中的计算结果；
- 经验证的一页一节结构；
- 官方目录页码范围和正文位置之间的校准结果。

任何推断页码都必须记录来源和可信度。

## 当前文件中的页码证据

### 第 1 卷 DOCX

已观察到：

- 页脚 XML 中存在 `PAGE` 字段。
- 正文 XML 中没有覆盖全书的显式页码标记。
- 正文没有 `lastRenderedPageBreak`。
- 正文只有 2 个显式分页符。
- 存在 938 个分节标记，绝大多数为默认下一页分节。
- 目录最大页码约为 940，与分节数量接近。

判断：

第 1 卷可能可以通过“分节结构 + 页码重启信息 + 目录校验”建立页码映射，但必须先验证。不能仅凭“第 N 个分节”直接断言是原书第 N 页。

### 第 2-10 卷 DOC

已观察到：

- 文件是 Word 97-2003 二进制 `.doc`，不是 OpenXML。
- 每个文件均有 `WordDocument`、`1Table`、`Data` 等核心 OLE 流。
- 核心流中能看到少量 UTF-16 `PAGE` 字段痕迹。
- 可见目录文本包含文献标题和页码范围。

判断：

这些信息不足以直接建立段落到原书页码的映射。`.doc` 内部的段落、样式、分节、字段、页眉页脚和排版结果需要专门解析或由 Word/LibreOffice 等排版引擎计算。

## 不可接受的页码替代方案

以下做法不能作为最终页码：

- 使用 PDF 第几页作为原书页码。封面、版权页、目录、插图页和重启页码会造成偏移。
- 使用 DOCX 第几个段落作为页码。
- 使用字符偏移或分词偏移作为页码。
- 使用目录中某文献的起止页来给该文献所有段落填同一个页码。
- 使用文件名、卷次或章节序号推算页码。
- 使用未经校验的 OCR 行号或截图页序号。

这些值可以作为内部定位辅助字段，但字段名必须明确为 `pdf_page_index`、`paragraph_index`、`char_offset` 等，不能命名为 `page` 或 `original_page`。

## 可靠路线

### 路线 A：从原书印刷页码建立页码锚点

这是最稳妥路线。

步骤：

1. 获取与 Word 文档同版的可靠 PDF、扫描件或带页码标记的文本源。
2. 抽取每页印刷页码，而不是使用 PDF 页序号。
3. 抽取每页开头和结尾的正文文本片段。
4. 与 Word 正文段落做文本对齐。
5. 生成 `page_anchor` 表：每个原书页码对应段落起止位置。
6. 对目录起止页、正文标题页、索引页进行抽样校验。

优点：直接面向“原书页码”。缺点：需要可靠原始页码来源。

### 路线 B：使用 Word/LibreOffice 排版引擎计算页码

适用于现有 Word 文件本身已经保留原书排版的情况。

步骤：

1. 在固定环境中只读打开 Word 文件。
2. 更新字段，但不修改原文档。
3. 对每个正文段落读取排版后的页码信息，例如 Word `Range.Information(wdActiveEndAdjustedPageNumber)`。
4. 同时读取节号、页眉页脚、`PAGE` 字段结果。
5. 导出段落到页码的映射表。
6. 与目录页码范围和正文标题做校验。

注意：

- 这一路线依赖排版引擎、字体和页面设置。环境变化可能改变分页。
- 必须记录工具版本、字体可用性和导出时间。
- 如果导出 PDF，只能把它作为排版结果的校验材料，不能直接把 PDF 页序号当作原书页码。

### 路线 C：第 1 卷的分节页码验证

第 1 卷 DOCX 特殊，因为它有接近逐页数量的分节标记。

可验证假设：

- 每个默认 `nextPage` 分节对应原书一页；
- `pgNumType start` 表示某些页码重启；
- 页脚 `PAGE` 字段在这些节中产生原书页码；
- 目录中的起止页与正文标题所在分节一致。

验证方法：

1. 统计每个分节的正文片段。
2. 用目录抽取的文献起始页与正文标题分节比对。
3. 抽样检查至少 30 个页码点，覆盖卷说明、正文、注释、索引。
4. 检查页码是否单调递增，是否存在重启、空白页、插图页。
5. 只有验证通过后，才能将 `section_break_inferred` 提升为可用页码来源。

结论：

第 1 卷可以优先探索这一路线，但输出必须保留 `page_source = section_break_inferred` 和校验状态，不能默认为真实页码。

### 路线 D：目录页码范围作为边界约束

目录可以可靠地提供“文献或章节的起止页候选”，但不能提供段落级页码。

可用场景：

- 建立文献清单；
- 识别文章边界；
- 校验标题所在页；
- 限定检索命中应该落在哪篇文献中。

不可用场景：

- 给每个段落赋精确页码；
- 判断引文命中的准确页码；
- 替代正文页码锚点。

## 推荐实施顺序

1. 不做搜索，先建立只读抽取管线。
2. 第 1 卷先验证分节是否对应原书页。
3. 第 2-10 卷选择排版引擎路线或寻找可靠 PDF/扫描页码来源。
4. 先从目录抽取文献清单和页码范围。
5. 生成段落文本、标题候选、分节信息、页码候选四类中间表。
6. 人工抽样校验后，再将页码写入可供搜索使用的正式索引。

## 页码来源分级

建议后续系统保留页码来源字段：

| 来源类型 | 含义 | 可用于最终页码 |
|---|---|---|
| `printed_page_marker` | 从印刷页码或页脚文字识别并与正文对齐 | 是 |
| `word_rendered_page` | Word/LibreOffice 排版后计算出的页码 | 是，但需记录环境 |
| `section_break_verified` | 分节与原书页经抽样验证一致 | 是，但需保留验证记录 |
| `section_break_inferred` | 仅由分节顺序推断 | 否，需验证 |
| `toc_range_bound` | 目录起止页约束 | 否，只能用于边界 |
| `paragraph_index` | 段落序号 | 否 |
| `pdf_page_index` | PDF 文件页序号 | 否 |
| `unknown` | 无页码来源 | 否 |

## 本阶段结论

在现有 Word 文件结构下，可靠获得原书页码需要额外的“页码锚点生成和校验”阶段。

第 1 卷 DOCX 可能有较强的分节线索，但仍需验证。第 2-10 卷 `.doc` 不能靠当前文件的表层读取直接给出段落页码。后续若要做到“准确页码”，必须引入排版引擎、可靠 PDF/扫描页码源，或能够完整解析 `.doc` 的工具链，并保留页码来源与可信度。
