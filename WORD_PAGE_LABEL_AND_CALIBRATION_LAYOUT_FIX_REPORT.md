# Word 页码标签与校准工作区布局修复报告

日期：2026-07-13

## 修复范围

本轮只修改了搜索结果中的引用页码展示、页码校准页面的前端布局与相关测试。未修改搜索算法、数据库结构、文献解析、导入、自动页码检测、页码映射算法、主题系统和引文格式规则。

## Word 页码显示

### 根本原因

搜索结果列表和详情页原来直接输出 `item.page`。Word 索引中的该字段保存为原始页码值，例如 `"4"`，因此界面只显示裸数字；中文脚注模块本身已有正确页码处理，所以复制出处并未发生同样的问题。

### 统一格式化

在 `src/me_finder/web.py` 中新增并统一使用：

- `formatCitationPageLabel(item)`：按来源类型选择可靠的引用页码字段；
- `formatChinesePageRange(start, end)`：处理单页、跨页、已有标签和缺失状态；
- `firstPageValue(values)`：按优先级读取非空页码值；
- `isUncalibratedPageLabel(value)`：统一识别未校准状态。

搜索结果列表、结果详情顶部和 PDF 页码详情现在使用同一个格式化入口。自动页码映射摘要与校准预览也复用该入口。

已验证的输出：

- Word `4` → `第4页`
- Word `4`–`5` → `第4—5页`
- 同页 `4`–`4` → `第4页`
- `序言第4页` → 保持 `序言第4页`
- PDF `citation_page=53` → `第53页`
- PDF 只有物理页序 → `页码尚未校准`

PDF 格式化不读取 `pdf_page_index`、`pdf_page_start_index` 或 OCR 内部页序作为引用页码。中文脚注和 GB/T 7714 仍由原有 `citations.py` 生成，测试确认继续使用当前命中页。

## 校准页面布局

### 分栏比例

详情打开时，校准内容区改为 CSS Grid：

```css
grid-template-columns: minmax(320px, 36fr) minmax(0, 64fr);
```

左侧文献区固定为单列紧凑卡片，右侧详情区获得主要工作空间。选中文献增加主题感知背景、强调色边框和 3px 左侧指示条。

Playwright 的实际尺寸测量如下：

| 窗口宽度 | 可用内容宽度 | 左侧宽度 | 右侧宽度 | 右侧占比 |
| --- | ---: | ---: | ---: | ---: |
| 1280px | 1060px | 381.59px | 678.41px | 64.0006% |
| 1440px | 1220px | 439.19px | 780.81px | 64.0010% |
| 1600px | 1380px | 496.80px | 883.20px | 64.0002% |

三种宽度下左侧卡片列数均为 1，页面均无横向溢出。左侧列表和右侧详情的 `overflow-y` 均为 `auto`，可以独立滚动。

### 窄窗口

窗口宽度不超过 1120px 时进入单面板模式：打开文献后隐藏左侧列表，详情区占校准内容区 100%；点击“返回文献列表”恢复列表。1100px 实测详情宽度为 880px，可用内容宽度也是 880px，比例为 100%。返回后搜索关键词仍保留。

### 映射表格

映射表格采用固定列布局，列名明确为：

- PDF 起始页；
- PDF 结束页；
- 引用起始页；
- 编号样式；
- 范围说明；
- 操作。

编号样式显示为“阿拉伯数字”“罗马数字（小写）”“罗马数字（大写）”“无编号”。删除操作改用 `currentColor` inline SVG。表格容器不再依赖横向滚动；打包版中使用“法哲学原理：或自然法和国家学纲要”的 3 个真实映射分段验证，1280px 下 `clientWidth=619`、`scrollWidth=619`，无横向溢出。

详情顶部保留唯一一个“自动检测页码”主按钮，原先自动检测区域中的同名重复按钮已移除。保存或应用映射后只刷新当前文献的卡片和详情，不再显示骨架屏或重新选择文献。

## 修改文件

- `src/me_finder/web.py`
- `tests/test_page_label_and_calibration_layout.py`
- `WORD_PAGE_LABEL_AND_CALIBRATION_LAYOUT_FIX_REPORT.md`

## 测试结果

- 完整自动测试：84 项通过；
- 真实 Word 检索：“宗教是人民的鸦片”在列表和详情均显示 `第4页`；
- 格式化运行测试：单页、跨页、已有 scope、同页、PDF 未校准全部通过；
- 1280px、1440px、1600px：右侧均约占 64%，左侧单列，无页面级横向滚动；
- 真实 3 行映射表：1280px 下无横向滚动；
- 1100px：详情区全宽，返回后保留搜索条件；
- 清霜蓝与深海夜截图检查通过；
- Windows 包已重建并实际启动，打包后端再次验证 `第4页` 和 64% 分栏规则。

验收截图：

- `build/word-page-label.png`
- `build/calibration-layout-1600.png`
- `build/calibration-layout-midnight-1440.png`

## Windows 构建

可执行文件：`dist/MEFinder/文献原句定位器.exe`

- 构建时间：2026-07-13 19:44:22
- 大小：3,906,683 bytes
- SHA-256：`F103B230AFDD346A7C0B5BBE2CC520A1702AFCC5CE8A0636A22BB5E9AE31EEA3`

## 已知限制

- 1120px 是当前单面板切换阈值；如果以后调整最左侧导航宽度，需要同步复核该阈值。
- 分栏开启有约 220ms 的既有宽度过渡；过渡完成后才达到最终 64% 或 100% 宽度。
- 当前 Windows 环境的窗口截图接口对 pywebview 返回 `0x80004002`，因此桌面壳层通过启动日志和打包后端进行验证；页面视觉截图由与 WebView 相同的本地前端在 Playwright Chromium 中完成。
