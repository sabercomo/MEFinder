# MEFinder 设计规范

2026-09-12：设计方向已确认。本文件用于指导后续界面实施，当前正式软件尚未按此规范改造。

## 1. 产品与设计目标

MEFinder 是本地优先的文献原句检索、页码定位、阅读与引文辅助工具。它帮助用户找到一句话、读清上下文、核对不同语言或版本，并获得可信的出处。

界面应当简洁、安静、好读。做减法的对象是重复的容器、说明和操作负担；字号、可点击区域、必要的上下文与定位证据应当保留。

产品定位保持不变，不因视觉改版扩展为知识库、项目管理或笔记工作台。不要为填满页面而增加概览卡片、统计指标、推荐内容或宣传文案。

### 已确认的方向

- 阅读形成完整的使用状态，正文占主要空间；需要对照时再加入第二个版本。
- 作品组围绕“一部作品有哪些版本、读哪一版、比较哪两版”组织。
- 设置优先呈现当前状态与下一步动作，编辑配置是有明确开始和结束的过程。
- 中性底色、少量强调色、轻分隔线，减少层层包裹的卡片。
- 控件借鉴 Apple Design 的熟悉感与即时反馈，以及 Emil Design Engineering 的细节一致性与克制动效。
- 视觉参考服务于 MEFinder 的实际任务，不绑定某个品牌，也不拼接多个产品的外观。

### 确认程度

设置讨论稿的整体布局与控件气质已获用户认可。阅读器和作品版本页已确认使用方向，但其完整页面与交互仍须结合真实软件细化。下文的尺寸是实施起点，最终通过实际窗口中的可读性与操作验证收敛。

## 2. 核心原则

### 内容优先

检索结果中的原句、上下文、文献名称和页码应容易辨认。阅读时，把书目信息、解析信息、导出操作放在明确的次级入口，避免长期占据正文两侧。

标题、版本名称、页码和来源必须来自真实数据。缺少出版方页码时如实说明，不生成看似精确的引用页码。对照中的缺失段落或未匹配状态同样必须可见。

### 用层级做减法

优先使用字体、距离和分隔线表达分组。普通设置项、文献条目、版本列表不逐条套大圆角卡片；卡片或独立面板用于确有边界的编辑任务、弹层或预览。

页面不能靠隐藏所有操作来显得简单。常用动作应直接可见，低频维护集中到具名入口，例如“编辑账号”“管理版本”“组件目录”。

### 状态可信

已保存、已检测、已安装、已启用、正在运行是不同的状态，不能互相代替。界面只呈现后端能够确认的状态；读取失败意味着未知，不意味着未安装。

### 轻操作立即反馈

开关改变应立即反映在控件上。保存、检测、安装等操作应在发起位置显示进度或结果。日常导航不等待装饰动画，键盘操作即时执行。

## 3. 页面结构

### 检索

- 搜索框是主要入口，查询模式与范围紧邻搜索框。
- 结果先显示命中原句及必要上下文，再提供文献、版本、页码和定位操作。
- 筛选与排序的视觉权重低于查询和结果，不铺满多层工具栏。
- 从结果进入阅读后，返回应恢复查询、筛选、选中结果与列表位置。
- 空状态说明用户下一步可以做什么，不增加与当前任务无关的模块。

### 阅读与对照

- 打开具体文献直接进入阅读，不强迫用户先经过作品管理页。
- 独立阅读窗口是完整的阅读空间，支持单独移动与缩放；不得为了统一导航重新把它包成主界面内的大弹窗。
- 顶部保留文献或作品名称、当前版本、目录入口、定位与阅读显示入口。
- 默认以一栏正文为主。选择对照版本后出现第二栏，两栏有清楚的版本与语言标识。
- 目录、书目信息、导出和解析详情按需打开；不默认同时展示多个侧栏。
- 两个版本各自保留自己的来源与页码锚点。排版位置或对齐关系不能替代引用证据。
- 对照应保持连续段落的阅读感，避免把正文默认画成密集表格。同步滚动、未匹配提示和当前对应段的强调服务于核对，不制造满页底色。
- 原始 PDF 与结构化正文的区别要说清楚；文字重排不改变原文的引用定位。

### 作品版本页

- 一部作品下列出可读版本，显示区分版本所需的信息，如语言、译者、出版信息与格式。
- 每个版本可以直接阅读；选定两个版本后提供明确的对照入口。
- 若需要生成对齐，应呈现真实准备状态，完成后进入阅读。已有可用对齐时避免重复设置。
- 版本改名、成员增减、基准调整集中到“管理版本”。
- 日常阅读和偶尔维护分开；管理能力保留，默认页面不成为配置表单。
- 作品分组不能改变源文献身份，也不能抹去各版本各自的书目与页码。

### 设置

设置采用目录与内容两区布局。默认建议进入设置后仅保留设置目录，通过“返回应用”回到之前的工作位置。保留完整应用侧栏可作为布局选择，最终结合主窗口导航验证；两套导航不应同时争夺主要空间。

设置正文最大宽度以 **680 px** 为起点，宽屏时不随窗口无限拉长。控件紧邻其说明，而不是贴住窗口最右缘。内容较少的页面允许留白。

“阅读与检索”可合并阅读窗口、PDF 打开方式、正文排版和繁简统一检索，避免单个开关独占一页。

译本对齐、MinerU API、本地 OCR 保留清楚的具名入口。其他解析 API、统计、引文格式、书目补全、外观、数据位置、文档传输、备份、更新等现有能力必须仍可找到。讨论稿中折叠或未展开的区域不代表可以删掉功能。

## 4. 视觉基础

### 色彩与表面

默认方向是中性浅色内容面、略有区分的侧栏、深色正文与克制的蓝色强调。强调色用于主要动作、选中状态与交互提示。成功、警告、错误颜色只表达对应语义，并配合文字。

深色与现有其他主题保持同样的层级关系。不得为实现浅色讨论稿而删除主题或把组件颜色写死。

| 用途 | 使用的项目 token | 视觉要求 |
| --- | --- | --- |
| 应用与侧栏背景 | `--app-bg`、`--sidebar-bg` | 安静、中性，通过轻微明度差分区 |
| 内容与浮层 | `--surface-primary`、`--surface-elevated` | 内容清晰，浮层有足够遮挡能力 |
| 正文与辅助说明 | `--text-primary`、`--text-secondary`、`--text-tertiary` | 辅助说明仍可读，不靠极浅灰隐藏信息 |
| 分隔与控件边界 | `--border-subtle`、`--border-control` | 页面分隔轻，控件边界可辨认 |
| 主要动作与选择 | `--accent-fill`、`--accent-contrast`、`--surface-selected` | 少量使用，不给所有按钮涂蓝 |
| 悬停与焦点 | `--surface-hover`、`--focus-ring` | 鼠标可感知，键盘焦点清楚 |

所有颜色最终落到 `src/me_finder/static/css/00-themes.css`。需要新 token 时先确认现有 token 无法表达其语义，再对各主题补齐。讨论稿的独立 CSS 变量不是生产命名规范。

不将玻璃、模糊背景、大阴影、渐变或弹跳作为“Apple 风格”的默认装饰。仅浮动层确有分离需求时引入材料感，并保持文字可读。

### 字体与间距

使用平台系统字体，中文优先系统中文字体。标题通过字号、字重与间距建立层级，正文保持自然字距；不要把英文标题的负字距机械套用到中文正文。

| 项目 | 起始规格 |
| --- | --- |
| 页面标题 | 23–24 px，中等偏重 |
| 设置项名称与常规正文 | 15 px，常规或中等字重 |
| 导航 | 14–15 px |
| 按钮与辅助说明 | 13–14 px；辅助说明不承担唯一的关键状态 |
| 阅读正文 | 18 px 起，可调整；行距约 1.75–1.9 |
| 常规界面行距 | 约 1.5–1.6 |
| 设置行上下留白 | 各 24 px 起，可在 18–30 px 范围内调整 |
| 设置内容水平内边距 | 24–48 px，随窗口收敛 |
| 设置目录宽度 | 约 180–200 px |

这些值不作为固定高度约束。换行、长文献名、字号放大和较小窗口应推动布局扩展，不能挤压文字或裁掉控件。

圆角复用项目 `--radius-*`：常规控件优先 `--radius-md`，较小控件可用 `--radius-sm`，开关用 `--radius-pill`。讨论稿中的约 7 px 控件圆角，实施时归并到现有 8 px 档，不新增相邻的圆角体系。

## 5. 控件规范

### 按钮

| 类型 | 典型用途 | 外观与行为 |
| --- | --- | --- |
| 主要按钮 | 保存账号、当前任务的主要提交 | 强调色实底，清楚的文字，当前操作组至多一个视觉主按钮 |
| 次要按钮 | 编辑、管理、重新读取 | 中性底色、轻边界、极轻阴影 |
| 轻按钮 | 返回、检测连接、局部修改 | 弱化边框与阴影，仍有明确点击区域 |
| 危险操作 | 移除账号、删除文献 | 文案明确指出对象，置于维护区域，按真实后果决定确认方式 |

- 桌面常规按钮高约 34 px，左右内边距约 12–14 px；表单控件高约 36 px。
- 同一操作组使用一致的高度、基线与间距，按钮宽度由文案决定。
- 触控环境提供至少约 44 × 44 px 的有效点击区域。
- 按下时立即有反馈，轻微缩放至 `0.97`；导航选项不整体缩放。
- 悬停只在支持悬停的精确指针设备生效，不放大按钮，不使邻居移位。
- 不依赖图标猜测核心动作；少量图标用于返回、展开等明确语义，不给每个按钮附加装饰图标。
- 禁用态必须有原因，且不会靠禁用藏起必要说明。

### 开关与单选

开关表达可立即切换的二元状态，例如独立阅读窗口、繁简检索、启用账号。采用小型滑动开关，轨道约 36 × 22 px，滑块约 16 px；保留原生表单语义、可访问名称与键盘操作。

开关的颜色和滑块位置共同表达状态。必要时附“开启 / 关闭”文字；如果行内已有准确状态，不重复堆叠徽章。整个标签区域可点击。

模型等互斥选项使用单选。名称、关键差异和已下载状态共同构成一个可理解的选项，避免同时出现“当前模型”徽章、选中底色、单选圆点等多重重复强调。

少量熟悉且简短的显示选项可用原生下拉菜单；需要比较后才能选择的内容保留并列单选及说明。下拉菜单不应只为节省高度而隐藏关键差异。

### 输入与表单

- 标签持续可见，placeholder 只提供示例，不代替标签。
- 必填与可选规则清楚，错误就近显示，保留已输入内容。
- 输入框使用轻边界；聚焦时有清楚的焦点环，不能去掉键盘焦点提示。
- 简单局部修改可就地展开；完整账号编辑进入独立的编辑区域，避免表单在长列表底部突然出现。
- 进入编辑后聚焦第一个合适字段；取消或关闭后把焦点还给发起操作的按钮。
- 保存和取消放在表单末尾同一组，保存靠右。自动保存的开关不额外配一枚“保存设置”。
- Escape 关闭当前编辑层或返回上一级；有未保存输入时须按实际丢失风险处理，不能默默丢弃重要修改。

## 6. 设置状态与操作流程

### API 服务与账号

概览显示服务地址、账号列表、启用状态、密钥保存状态、最近有效的检测结果以及到期信息。解析数量等低频信息进入统计或账号详情，不让表格承载所有数据。

服务地址默认以可读文字显示，点击“修改”后出现输入框。多个账号分别编辑、启停与检测，不退化成全局唯一的 API Key 输入框。

账号编辑顺序：账号名称 → Token → 可选到期日期 → 保存。密钥不回显，修改规则必须说明清楚；编辑时留空可保留既有密钥，新增账号按实际服务要求填写。

| 状态 | 界面含义与动作 |
| --- | --- |
| 无账号 | 说明用途，提供“添加账号” |
| 密钥已保存、尚未检测 | 如实显示，提供“检测连接” |
| 检测中 | 在检测按钮附近显示进行状态，避免重复提交 |
| 检测成功 | 显示最近检测结果，不承诺服务永久可用 |
| 检测失败 | 给出后端可确认的原因与重试或编辑入口 |
| 账号停用 | 不分配新的解析任务；已有任务的行为遵循后端契约 |

密钥、地址等影响连接的字段变化后，旧检测结果失效；单纯改账号名称不应伪装成连接故障。保存成功不等于连接成功。

### OCR 与本地服务

文字识别引擎、本地 MinerU 部署、通用本地模型分别管理。名称下方优先说明它适合的文献类型，再呈现引擎名与版本等次要信息。

| 状态 | 首要呈现 | 主要动作 |
| --- | --- | --- |
| 状态读取失败 | 状态未知及读取错误 | 重新读取 |
| 未安装 | 用途、实际下载信息 | 下载安装 |
| 下载或安装中 | 真实阶段与进度 | 仅显示后端支持的控制动作 |
| 安装失败 | 失败阶段与可理解的原因 | 重试、查看详情 |
| 已安装、未启用 | 明确说明尚未使用 | 启用 |
| 已启用 | 说明何时被用于导入 | 停用、管理 |
| 已配置、未启动的服务 | 配置与运行状态分别呈现 | 按后端能力提供启动或连接管理 |

未安装时先完成安装，不摆放一个看似可用的启用开关。未知状态不能按“未安装”处理，也不能允许基于错误状态触发重复安装。

路径、运行时、手动配置、日志和验证详情放在高级区域。自动选择如何使用引擎，以及失败后是否调用 MinerU，应按真实行为说明；涉及网络服务的路径不能描述为全程离线。

模型大小、硬件要求和质量描述必须来自实际组件元数据或实证报告。不得用“更准”等未经验证的文案替代选择依据。

## 7. 动效与反馈

动效用来确认输入与解释状态，默认保持短、轻、可打断。没有拖拽或动量交互时，不为使用弹簧而引入动画依赖。

| 交互 | 规则 |
| --- | --- |
| 鼠标按下按钮 | 立即响应，约 120 ms 的轻微缩放反馈 |
| 开关滑块 | 约 160 ms，平滑移动，无弹跳 |
| 页面与目录切换 | 即时，避免整页淡入或逐项入场 |
| 键盘发起操作 | 即时，不等待位移或缩放动画 |
| 偶发浮层 | 如确有需要，约 150–220 ms；锚定触发位置，进入退出路径一致 |
| 保存、检测、安装结果 | 优先更新发起位置，重要错误保留至用户能处理 |

简单反馈可使用 `cubic-bezier(.23, 1, .32, 1)`。仅声明实际需要过渡的属性，禁止 `transition: all`。不要动画化布局尺寸来装饰按钮，也不要延迟真实状态以展示假进度。

支持减少动态效果：移除缩放、滑动与弹跳，保留即时状态变化。支持高对比度；如将来使用透明材料，还要提供不透明替代。

## 8. 自适应与可访问性

- 优先保证真实桌面窗口中的体验，同时检查窄窗口与系统字号放大。
- 控件放不下时换行或调整布局，不把整页同比缩小。
- 窄窗口中的设置目录可收纳为选择入口，所有设置分类仍可访问。讨论稿对未展开内容的省略不是生产行为。
- 侧栏、主内容与弹层的滚动职责明确，不因固定高度裁掉保存按钮。
- 使用原生按钮、输入、单选、选择框及正确语义；支持 Tab、Shift+Tab、Enter、Space 和适用的 Escape 行为。
- 正文与必要辅助文字必须在实际背景上可读，状态不只靠颜色区分。
- 动态结果提供适度的读屏通知；输入密码不回显或写入日志。

## 9. 实施边界与验收

本文件描述目标，不证明界面或后端行为已经实现。讨论稿中的示例账号、检测结果、安装状态和点击反馈不是生产数据或真实执行结果。

后续实施遵循 `AGENTS.md`。沿用原生 JS/CSS、现有主题和组件边界；局部讨论稿样式不得整段覆盖全局控件。静态资源、偏好或全局符号发生变化时，同步相应守卫、快照与指纹。

建议先在设置页落地已确认的空间与控件规范，再实现账号和 OCR 状态流程；阅读器与作品版本页在核对真实使用链路后单独推进。每次只处理一个可验证的主题。

### 完成标准

- 在真实软件中核对修改前后的页面，所有原有必要入口仍可找到。
- 标题、控件、行距、边界与主次按钮一致，宽屏不会把名称和操作拉得过远。
- API 测试覆盖保存与检测的区别；OCR 覆盖未知、未安装、安装中、失败、已安装与启用状态。
- 长标题、长错误信息、窄窗口、浅色与深色主题均无关键内容裁切。
- 键盘焦点顺序清楚，编辑后可以返回发起位置；减少动态效果时仍有明确反馈。
- 检索到阅读再返回，查询和位置可恢复；阅读与对照保留真实来源和定位证据。
- 按仓库要求完成适用测试、lint、前端守卫与实施收尾，再报告该主题完成。

### 设计依据

本规范来自用户认可的 MEFinder 设置讨论稿及控件细化稿，并结合本机实际设置页面校正。Apple Design 用于熟悉的控件语义、空间关系、字体与即时反馈；Emil Design Engineering 用于按压细节、动效取舍、焦点连续性和组件一致性。它们是设计方法参考，本文件不是 Apple 官方规范，也不要求复刻任一品牌界面。
