# 页码校准文献库界面重构报告

## 完成情况

“页码校准”页面已从原生 PDF 下拉框改为默认加载全部 PDF 的响应式卡片库。原有页码映射算法、自动检测服务、分段编辑器、搜索服务和 PDF 解析流程均未重写；映射编辑器被保留在卡片右侧详情抽屉中。

正式桌面包：`G:\ME_Finder\dist\MEFinder\文献原句定位器.exe`

## 修改文件

- `src/me_finder/web.py`
  - 新页码校准卡片库、工具栏、状态 Tab、排序、搜索、右侧详情、加载与空状态。
  - “更多”菜单和“从文献库移除”双阶段确认弹窗。
  - 新增 `/api/calibration-library` 和 `/api/documents/remove`。
  - 自动检测时卡片即时显示“正在检测”，检测结束后显示待确认或检测失败；应用映射后从数据库刷新。
- `src/me_finder/calibration_library.py`
  - 从来源记录、PDF profile、自动映射、人工配置和导入记录生成卡片状态与统计。
- `src/me_finder/document_deletion.py`
  - 统一的单文献移除服务；协调配置、数据库事务、缓存暂存和可选文件清理。
- `src/me_finder/database.py`
  - 新增单来源事务删除，更新全库统计，不重建全文索引。
- `tests/test_calibration_library_ui.py`
  - 状态分类、卡片结构、安全移除、PDF 保留、副本显式删除和 Word 隔离测试。

## 页面结构

页面顶部实时显示：

- PDF 总数
- 已校准
- 待校准
- 待确认
- 检测失败

控制区包含：

- 书名、作者、译者、出版社和文件名联合搜索；忽略大小写与空白。
- 全部、已校准、待校准、待确认、检测失败五类状态筛选。
- 导入时间、书名、作者、最近修改时间、校准状态排序。
- 升序、降序方向和刷新按钮。

卡片按窗口宽度显示 3、2 或 1 列。打开右侧详情后网格自动收窄，搜索词、筛选、排序和滚动位置不会因选择文献而重置。

每张卡片显示稳定标题、作者、PDF/MinerU、页数、文件大小、导入时间、校准状态和映射摘要。无效 `??????` 元数据不会参与正常排序或显示，会回退到稳定标题或原文件名。

## 状态判断

状态不是按“是否有 mapping 行”粗略判断，而是综合：

- 配置中的人工分段与 `validated_by`
- `pdf_profile.mapping_status`
- 自动映射的置信度和应用区间
- 自动检测失败原因
- 当前导入/检测任务
- 原始来源文件是否存在

映射如下：

- `manual_mapped` → 已校准 · 人工映射
- `auto_mapped_high` → 已校准 · 自动映射
- `auto_mapped_medium` / `needs_review` → 待确认
- 无映射 → 待校准
- `auto_mapping_failed` → 检测失败
- 检测任务运行中 → 正在检测
- `source_missing` → 原文件缺失，并归入检测失败筛选

当前真实运行库验收：PDF 11 本，已校准 10 本，检测失败 1 本。待校准和待确认为 0，但对应筛选使用合成状态测试覆盖。

## 拼音排序

前端使用现代 WebView 内置的：

`Intl.Collator('zh-CN-u-co-pinyin')`

中文按拼音，英文不区分大小写按字母排序；中文组位于英文组之前，空值最后。排序只生成视图顺序，不修改数据库标题。

验收样例顺序：

1. 批判理论
2. 食人资本主义
3. 消费社会
4. 自由的权利
5. Critique of Forms of Life
6. Reconceiving Social Philosophy

## 右侧详情

点击卡片后打开与文献库一致的右侧详情抽屉，显示：

- 标题和作者
- PDF/MinerU 类型与页数
- 当前校准状态
- 映射区间和分段编辑器
- 自动检测预览、异常与映射依据
- 自动检测、手动设置、查看依据、打开原文
- 详情底部的“从文献库移除”危险操作

关闭详情只收起抽屉，列表状态和滚动位置保持不变。

## 从文献库移除

UI 不再使用“删除 PDF”文案。默认操作会移除：

- `source_files`
- 该来源的 `paragraphs` 和搜索索引
- `page_anchors`
- `pdf_pages`
- `pdf_page_mappings`
- `pdf_import_runs`
- `audit_issues`
- 该来源的 volume、work 和 TOC 记录
- `pdf_imports.json` 中对应配置、映射和书目元数据
- 用户选择的专属解析快照和 MinerU 产物

数据库删除在单一 SQLite 事务中执行，并在删除前自动备份数据库。配置写入或数据库事务失败时，配置和已暂存文件会恢复，UI 不会提前隐藏卡片。

删除成功后后端重新加载搜索引擎，前端立即移除卡片、更新统计、发布 `library_changed`，并在必要时重新执行当前搜索。

## 文件安全

默认始终保留 PDF 文件。

只有来源位于应用自己的 `corpus/raw_pdf` 时，弹窗才提供“同时删除应用内保存的 PDF 副本”；该选项默认不勾选。勾选后必须再次点击“确认移除并删除副本”。服务端再次校验真实路径必须位于应用目录，任何外部路径都不能删除。

解析缓存默认可清理，但 MinerU manifest 或结果目录若被其他文献配置引用，会视为共享数据并保留。不能明确归属的产物不会猜测删除。

## 测试与验收

- 全套 58 项自动测试通过。
- JavaScript 经 Node `--check` 语法验证。
- 使用临时 SQLite 实际执行默认移除：PDF 文件保留，PDF 搜索结果消失，Word 来源与 Word 搜索正常。
- 使用临时 SQLite 实际执行显式副本删除：只有应用内部 PDF 副本被删除。
- 正式 exe 启动成功，并从 `%LOCALAPPDATA%\MEFinder\runtime` 加载真实数据库。
- 正式 exe 的 `/api/calibration-library` 返回 11 本真实 PDF 及正确统计。
- 《消费社会》返回 MinerU、246 页、自动映射、`PDF 第 7 页 → 第 1 页 · 共 2 段`。
- 正式页面 HTML 已确认不存在 `cal-doc-select`，包含卡片网格、状态筛选和安全移除选项。

没有用真实文献执行删除验收，避免破坏用户语料；删除完整链路使用结构等价的临时数据库和文件目录验证。

## 已知限制

- 较早导入且缺少 `pdf_import_runs.started_at` 的文献，导入时间会回退为索引中保存的文件修改时间。
- 拼音排序依赖桌面应用使用的现代 WebView `Intl.Collator`；当前 Windows WebView/Node 验收环境支持该规则。
- 现有“保存校准配置”仍沿用原项目的全索引刷新行为，本轮没有改写页码映射应用流程。
- pywebview 窗口不支持当前 Windows 截图接口，视觉验收采用正式应用 API、HTML/JS 语法和响应式 CSS 结构检查；未使用不可靠的坐标自动化。
