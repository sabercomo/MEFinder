# 中文脚注、GB/T 与书目元数据修复报告

## 完成情况

本轮已修复中文脚注与 GB/T 图书引文导出，增加 PDF 前置页书目信息识别、人工编辑、识别依据和历史 PDF 即时更新。GB/T 缺少关键字段时不再拼接残缺格式；两种格式都只使用 `citation_page` / `citation_page_label`，不会使用 PDF 物理页或 OCR 页序。

## 修改文件

- `src/me_finder/citations.py`
  - 普通专著、译著和跨页格式按新规则输出。
  - GB/T 缺关键字段返回 `metadata_incomplete` 和缺失字段。
  - 未校准页码时拒绝生成可靠脚注或 GB/T。
- `src/me_finder/bibliographic_metadata.py`
  - 新增 PDF 前 20 页书目元数据识别。
  - 识别作者、译者、出版社、出版地、出版年份、ISBN。
  - 保存字段级页码、证据文本、来源和置信度。
  - 人工元数据默认不被自动识别覆盖。
  - 支持单篇 SQLite 元数据即时更新。
- `src/me_finder/pdf_extractors.py`
  - PDF 配置中的书目字段写入 source metadata。
- `src/me_finder/search.py`
  - 引文格式优先使用 source 的规范书目元数据。
- `src/me_finder/web.py`
  - 文献库详情新增“书目信息”编辑区。
  - 新增自动识别、重新识别并覆盖表单、保存、查看依据。
  - 搜索结果元数据不完整时阻止复制伪 GB/T，并提供补全入口。
  - 保存后热重载 SearchEngine，无需重启或重新全文索引。
- `config/pdf_imports.json`
  - 补全《食人资本主义》的经过确认的人工书目元数据。
- `tests/test_citations.py`
  - 增加严格译著/专著、跨页、缺字段、页码安全和版权页识别测试。
- `tests/test_runtime_page_mapping.py`
  - 增加元数据保存后搜索引文即时更新测试。

## 格式规则

### 中文脚注

译著：

`作者：《书名》，译者译，出版社，出版年份，第X页。`

普通专著没有译者字段。跨页使用中文连接号，例如“第197—198页”。

### GB/T 7714

译著：

`作者. 书名[M]. 译者, 译. 出版地: 出版社, 出版年: 页码.`

跨页使用半角连字符，例如 `197-198`。

普通图书 GB/T 必需字段：作者、书名、出版地、出版社、出版年份和引用页码；译著还必须有译者。缺少任一字段时返回明确错误和 `metadata_incomplete`，不再输出类似：

`南希·弗雷泽. 食人资本主义[M]:111.`

## 页码安全

引文页码只读取搜索命中记录中的：

1. `citation_page_label_start/end`
2. `citation_page_start/end`

如果引用页未校准：

- 中文脚注：`该文献页码尚未校准，不能生成可靠脚注。`
- GB/T：`该文献页码尚未校准，不能生成 GB/T 引文。`

测试覆盖 `citation_page=197`、`pdf_page_index=227`，导出只能使用 197。

## 元数据识别

自动识别默认扫描已有索引中的前 20 个 PDF 页面。优先使用：

1. 可信 PDF 内嵌 metadata；
2. 已提取页面文本；
3. 已有 MinerU 页面文本；
4. 书名页、版权页、CIP 页和封面文本。

不会为识别元数据重新 OCR 全书。出版社缺少明确出版地时，只对稳定映射表中的出版社进行低置信度城市推断，并保存 `inferred_from_publisher` 依据。

人工保存后：

- `metadata_source = manual`
- `metadata_confidence = 1.0`
- 自动识别默认直接保留人工值。
- 只有点击“重新识别并覆盖表单”并确认后，才会把自动结果载入编辑表单；仍需点击保存。

## UI 与即时生效

文献库 PDF 详情现在显示：作者、书名、译者、出版地、出版社、出版年份、ISBN、元数据状态和来源。

按钮包括：

- 自动识别书目信息
- 重新识别并覆盖表单
- 保存
- 查看识别依据

保存只更新当前 PDF 的配置、source、volume、work 和 paragraph metadata，然后热重载搜索引擎。不会重新 OCR，也不会重建全文索引。

## 《食人资本主义》实际验收

前置页实际可读取到：

- PDF 第 1 页：`［美］南希·弗雷泽著蓝江译`
- 作者：南希·弗雷泽
- 译者：蓝江

出版社行在当前文本层存在 OCR 损坏，出版年份未出现在已有前置页文本，因此系统没有假装自动识别。根据用户确认的书目信息，以人工元数据补全：

- 出版地：上海
- 出版社：上海人民出版社
- 出版年份：2023
- 元数据来源：manual

真实搜索命中验证：

- `pdf_page_index = 217`
- `citation_page = 197`

中文脚注：

`南希·弗雷泽：《食人资本主义》，蓝江译，上海人民出版社，2023年，第197页。`

GB/T：

`南希·弗雷泽. 食人资本主义[M]. 蓝江, 译. 上海: 上海人民出版社, 2023: 197.`

开发索引和 `%LOCALAPPDATA%\MEFinder\runtime` 实际运行索引均已验证。运行库仍保留 21 个来源文件，包括《247晚期资本主义与睡眠的终结》。

## 测试结果

- 50 项自动测试通过。
- 覆盖完整译著、普通专著、跨页、缺出版地/出版社/年份、未校准页码、PDF 物理页禁止回退、版权页识别、人工优先和即时搜索更新。
- JavaScript 通过 Node 语法检查。
- 元数据 GET 和自动识别 HTTP API 已用《食人资本主义》实际验证。

## 已知限制

- 当前自动识别依赖已有文本层或 MinerU 结果；没有任何文本/OCR 的版权页只提示人工补全，不自动重新 OCR 全书。
- 版权页 OCR 严重损坏时，出版社和年份可能保持缺失或待确认。
- 当前只实现普通专著与译著 `[M]`；编著、主编、论文集析出文献不在本轮新增范围。
