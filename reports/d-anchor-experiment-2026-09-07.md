# D 锚点排查：n53（成功）与 n41（排除），2026-09-07

issue #18 下一轮：查锚点是否把正确目标隔在走廊外。生产库、正式算法磁盘文件全程不改；候选规则
仅在实验驱动里 monkeypatch `_validate_soft_anchors` 后跑**真实** `generate_alignment`（含 DP +
注释通道 + 段质量门），所以「修好」是通过完整后处理的真实 accepted 输出，不是 raw-DP 假象。
驱动 `scripts/d_anchor_experiment.py`；证据 `reports/d-anchor-experiment-2026-09-07.json`（R9）、
`reports/d-anchor-regression-2026-09-07.json`（16 pair 回归）。

## 1. 核实 n53：确认是错误软锚点，非边界/分段/版本顺序

- pivot（JA 547）`これはひとつの作品世界にのみべったり惚れ込んでしまうのではなく、どこか醒めつつ熱狂…`
  正确译文就是 ZH **T562**「正因如此，他们并不会完全沉迷于某个单一的作品世界，而是表现出一种清醒
  而又狂热的姿态」——逐段相似度 **0.928**，金标属实。
- pivot 走廊 `[458,574)×[579,583)`：左界锚点 **P457→T578 `name:权威`**、右界 P574→T583。该走廊把
  116 个 JA 段挤进 4 个 ZH 段，且 547>457 却应对 562<578——**违反单调性**。
- 病根锚点 **P457→T578 `name:权威`**：JA457「最も**権威**ある虚構」被按共有词「权威」配到 ZH578
  「…绝对**权威**…」（**不同句**），锚点余弦仅 **0.836**（全书 25 个软锚点里最低、刚过 0.83）；而
  JA457 的真译是 ZH**503**「现实或可谓最有力的虚构」（**0.923**，「権威ある」被意译成「有力」，词
  没转移）。即典型的 term/name 同词异义假锚点。**不是**正文边界、分段映射或版本顺序问题。

## 2. 最小反事实：只去掉该锚点，区域自愈

在副本上仅移除 `权威` 锚点、把走廊并到相邻**有效**锚点（神秘体验 451→496 / 东京都知事 574→583）
之间重跑真实 DP：该区从退化（116:4、大量 unmatched）变为 **48/48 全部 accepted**；JA457 归位到
T501–502（真值 ~503），pivot 547 由 **unmatched → accepted**。（此步人为移除仅证明**原因**，不计修复。）

## 3. 自动识别规则（不写死位置）：迭代式 leave-one-out 软锚点校验

规则：对每个软锚点（`name:/term:/number:`），暂时移除、在其相邻锚点界定的走廊上重跑真实 DP、量其
源段被重新落到的位移；好锚点位移≈0，假锚点跳数十段。移除位移最大者、重算、迭代（让被连累的邻居自愈）。
纯文本/embedding/DP，**不含夹具编号、位置或金标**。R9 全 25 软锚点实测：好锚点位移 ≤2，`权威`=**77**、
`土萌`=21、`神秘体验`=65（**被 权威 连累**——移除 权威 后其位移降到 1，故不误删）。用**真实每段长度**
（单位长度会虚高位移、过度删除），阈值 **50**（清晰离群与边界样本 ≤36 之间）。

## 4. 交付判断

**n53：确有收益的候选修复。** 规则在**完整流水线**上只删 `权威`，R9 accepted **1773→1811（+38）**、
改动全部局限在坏锚点邻域；pivot 547 由 unmatched → **accepted（P545–547→T559–560, 0.927）**，落在正确
段落（残留约 2 段的分组相位偏移，属 n28/n80/n98 那类近似并列，与锚点正交）；#17 修好的 **n11 保持正确**
（→T3163–3164, 0.942）。

**回归安全（16 pair 全跑完整流水线）**：规则只在 2 个 pair 触发移除——R9 的 `权威`(77) 与 DE→ZH 的
`number:1642`(55，附带轻微改善 1998→2001、无害)；其余 14 pair **0 移除**。**60 条正确金标 + n74 共 61 条
逐条比对最终 accepted 状态：0 回归**（`reports/d-anchor-regression-2026-09-07.json`）。

**n41：排除锚点病因。** 同一规则在 hegel→epub 上**移除 0 个锚点**、n41 不变（仍 →T5119–5120），其正确
目标 6503–6508 距落库落点约 1382 段、跨 6 段——不是本类假软锚点，是别的现象（疑版本顺序差异或金标口径），
留待单独排查，**不为它放开全书搜索**。

**候选修复（提交审查，未合入）**：把上述迭代 leave-one-out 软锚点校验加进
`semantic_alignment._validate_soft_anchors`（现有 sim/ratio 门之后追加一段）。证据显示它安全（61 金标 0
回归）、有效（修好 n53 的锚点病灶）。因涉及正式算法，按要求**只作候选供审查**，本轮未改磁盘上的
production 代码；`number:1642` 属阈值 55 的边界移除，合入前建议连同阈值一起复核。

## 说明：验收口径较上一轮的改进

本轮「accepted」取自真实 `generate_alignment` 的最终 `review_status`（过完注释通道+质量门），不再以
相似度过线近似；金标逐条比最终状态；不再用 `len(gold)>3` 判「范围」而据此排除（n41 是按**证据**（规则
0 移除）排除，非按段数）。上一轮 gap 报告结论已收窄为「当前检测器+这几档折扣未修好」，不外推为普遍不可识别。
