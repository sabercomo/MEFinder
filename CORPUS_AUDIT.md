# 马恩文献语料库结构审计

审计日期：2026-07-05

## 结论摘要

`corpus/raw_docx/` 目前包含《马克思恩格斯文集》10 卷，每卷 1 个文件，但并不是全量 DOCX。第 1 卷是 `.docx`，第 2-10 卷是 Word 97-2003 二进制 `.doc`。这意味着后续不能把整个目录当作统一的 OpenXML 语料处理。

第 1 卷 DOCX 中存在大量分节标记和页脚 `PAGE` 字段，结构疑似保留了逐页排版信息；但正文段落本身没有可靠的显式原书页码字段。第 2-10 卷 `.doc` 可确认包含 `WordDocument`、`1Table`、`Data` 等 Word 二进制核心流，也能看到少量 `PAGE` 字段痕迹，但在不使用 Word 二进制解析器或排版引擎的情况下，无法建立正文段落到实际页码的可靠映射。

## 文件清单

| 卷次 | 文件名 | 格式 | 大小 |
|---:|---|---|---:|
| 1 | `《马恩文集》第1卷（2010.9.6)(1).docx` | DOCX/OpenXML | 1.12 MB |
| 2 | `《马恩文集》第2卷(2010.9.7)(1).doc` | DOC/OLE | 2.16 MB |
| 3 | `《马恩文集》第3卷（2010.9.13)(1).doc` | DOC/OLE | 1.99 MB |
| 4 | `《马恩文集》第4卷（2010.9.8）(1).doc` | DOC/OLE | 2.16 MB |
| 5 | `《马恩文集》第5卷（二版全集44卷 2010.9.17)(1).doc` | DOC/OLE | 3.00 MB |
| 6 | `《马恩文集》第6卷（二版全集45卷 2010.10.13)(1).doc` | DOC/OLE | 2.37 MB |
| 7 | `《马恩文集》第7卷（二版全集46卷 2010.10.21)(1).doc` | DOC/OLE | 5.61 MB |
| 8 | `《马恩文集》第8卷 2010.11）(1).doc` | DOC/OLE | 2.29 MB |
| 9 | `《马恩文集》第9卷 2010.11.2）(1).doc` | DOC/OLE | 2.03 MB |
| 10 | `《马恩文集》第10卷 （2010[1].12.28）(1).doc` | DOC/OLE | 3.16 MB |

统计：

- 文件总数：10
- `.docx`：1 个
- `.doc`：9 个
- 目录组织：所有卷文件平铺在 `corpus/raw_docx/` 下，没有按卷次分子目录。
- 命名规律：基本为 `《马恩文集》第N卷...`，但括号、日期格式、全角半角标点不统一；第 5-7 卷文件名额外标注“二版全集44/45/46卷”。

## 代表性文件结构

### 第 1 卷 DOCX

第 1 卷是唯一可直接拆解 XML 的文件。

观察到的 OpenXML 结构：

- 包内 XML/关系部件共 35 个。
- 主要部件包括 `word/document.xml`、`word/styles.xml`、`word/numbering.xml`、`word/settings.xml`、`word/footnotes.xml`、`word/endnotes.xml` 和多个 `word/footer*.xml`。
- 正文段落数：8788。
- 段落样式高度不统一：8772 段没有显式 Word 样式；`heading 1` 仅 6 段，`heading 2` 1 段，`标题3` 3 段，`标题4` 4 段，`Subtitle` 2 段。
- 段落对齐：居中 838 段，右对齐 26 段，其余大多无显式对齐。
- 正文显式分页符：2 个。
- `w:lastRenderedPageBreak`：0 个。
- 分节标记：938 个段落内 `sectPr`，另有最终节属性；绝大多数为默认 `nextPage`，1 个为 `continuous`。
- 页脚部件：`footer1.xml` 到 `footer14.xml`，均可见 `PAGE` 字段。

标题样例：

- 封面和说明类标题多为居中普通段落，例如“马克思恩格斯文集”“编　辑　说　明”。
- 目录标题“目　　录”使用 `heading 1`。
- 正文部分有的文章标题使用 `heading 1`，例如“第一卷说明”“《黑格尔法哲学批判》导言1”“论犹太人问题 16”“*1844年经济学哲学手稿61”。
- 大量正文内小标题、章标题、图题是居中普通段落，例如“布鲁诺·鲍威尔：《犹太人问题》”“英　国　状　况”“第　四　章”等。
- 标题文本尾部的数字常常是注释号或排版残留，不能直接解释为页码。

页码相关观察：

- 页码字段位于页脚，而不是正文段落。
- 正文中的字段主要是公式类 `EQ \F(...)`，没有发现正文 `PAGE` 字段。
- 只有 2 个显式分页符，不足以表示全书分页。
- 纯数字居中段落有 32 个，分布不连续，不能当作页码序列。
- 938 个分节标记数量接近第 1 卷目录最大页码 940，疑似每页一个分节，但这需要以排版引擎或人工抽样校验后才能作为页码来源。

### 第 2 卷 DOC

第 2 卷是 Word 二进制 `.doc`。

OLE 核心流：

- `WordDocument`：1,844,463 字节
- `1Table`：386,484 字节
- `Data`：7,629 字节
- `Equation Native` 流：7 个
- UTF-16 `PAGE` 字段痕迹：8 处

可见文本样例显示，第 2 卷包含目录、卷说明和多篇独立文献，例如“共产党宣言”“危机和反革命”“资产阶级和反革命”“1848年至1850年的法兰西阶级斗争”“德国农民战争”“德国的革命和反革命”等。

### 第 5-7 卷 DOC

第 5-7 卷分别为《资本论》第一、二、三卷，文件名中标注对应《马克思恩格斯全集》二版第 44-46 卷。

这些卷不是普通短文合集，而是大型专著结构：

- 第 5 卷：`WordDocument` 2,392,592 字节，`1Table` 658,924 字节，`Equation Native` 25 个。
- 第 6 卷：`WordDocument` 1,383,414 字节，`1Table` 521,908 字节，`Equation Native` 307 个。
- 第 7 卷：`WordDocument` 2,974,877 字节，`1Table` 1,243,895 字节，`Equation Native` 688 个。

可见文本显示这三卷目录层级更深，包含“篇、章、节”结构和大量公式对象。后续识别边界时不能只按“文章标题”处理，还要支持专著内章节层级。

### 第 9 卷 DOC

第 9 卷可见内容包括《反杜林论》和《自然辩证法》两大文献，以及卷说明、索引等。标题边界需要识别大文献、编、章、节多层结构。

### 第 10 卷 DOC

第 10 卷为书信选编。

OLE 核心流：

- `WordDocument`：2,287,326 字节
- `1Table`：959,517 字节
- `Data`：16,699 字节
- `Equation Native` 流：24 个
- UTF-16 `PAGE` 字段痕迹：10 处

可见目录显示第 10 卷包含大量独立书信，标题形式通常为“编号.作者致收信人（日期）”，并按年份分组。后续边界识别要支持“年份分组 + 编号书信标题”的模式。

## 文献标题表示方式

当前语料没有统一可靠的单一标题规则。

观察到的标题来源包括：

1. Word 标题样式
   - 第 1 卷少量使用 `heading 1`、`heading 2`、`标题3`、`标题4`、`Subtitle`。
   - 这些样式覆盖率很低，不能只靠样式识别标题。

2. 普通段落
   - 大多数正文段落无显式样式。
   - 许多文章标题、小标题、图题也是普通段落。

3. 居中与字体格式
   - 正文标题常用居中、较大字号、黑体/华文中宋/仿宋等字体组合。
   - 作者行常与标题分离，例如先出现“卡·马克思”或“弗·恩格斯”，下一行或后几行才是文献标题。
   - 多行标题常见，不能逐段独立识别。

4. 目录
   - 目录中标题与页码范围较完整，例如“卡·马克思　论犹太人问题 … 21-55”。
   - 目录可作为后续建立文献清单和边界候选的高价值来源。
   - 目录项中含点线、页码范围、章节层级，需要专门解析。

5. 数字尾注
   - 标题尾部数字如“导言1”“论犹太人问题 16”“国民经济学批判大纲 25”可能是注释号、脚注号或其他排版文本，不能无条件当页码。

## 页码来源审计

### 正文显式页码

第 1 卷 DOCX 正文没有发现可覆盖全书的显式页码标记。纯数字居中段落只有 32 个，且不连续，不能作为页码。

第 2-10 卷 `.doc` 的原始二进制中可见目录页码范围和少量 `PAGE` 字段痕迹，但这不等于正文段落携带了页码属性。

### 分页符

第 1 卷 DOCX 只有 2 个 `w:br type="page"`，不足以建立全书页码映射。

第 2-10 卷 `.doc` 未在本阶段解析到段落级分页符；需要 Word 二进制解析器或排版引擎读取。

### PAGE 字段

第 1 卷 DOCX 的 `PAGE` 字段位于页脚部件中。

第 2-10 卷 `.doc` 的 OLE 核心流中均发现少量 UTF-16 `PAGE` 字段痕迹，但无法从这些痕迹直接推导“某正文段落属于某原书页码”。

### 页眉页脚

第 1 卷 DOCX 包含多个页脚 XML，页脚内有 `PAGE` 字段。字段说明 Word 可以在排版时计算页码，但 XML 本身没有把计算结果写回每个正文段落。

### DOCX XML 能否建立“正文段落-实际页码”映射

对第 1 卷：可能存在“基于分节的页码推断路径”，但当前不能直接判定为可靠映射。

依据：

- 938 个分节标记接近目录最大页码 940。
- 绝大多数分节为默认下一页分节，疑似每页一个分节。
- 若经校验证明“一个分节等于一个原书页”，则可用分节顺序、`pgNumType start` 和目录页码进行映射。

限制：

- OpenXML 的 `sectPr` 是节属性，不是显式“原书页码锚点”。
- 没有 `lastRenderedPageBreak`。
- 页脚 `PAGE` 字段需要 Word 排版后才有确定结果。
- 分节可能出现在目录、插图、索引和正文中，且存在重启页码的 `pgNumType`。

对第 2-10 卷：不能从现有 `.doc` 文件直接建立段落到实际页码的可靠映射。原因是 `.doc` 是二进制 Word 格式，段落、样式、分节、字段、页眉页脚均需要专门解析；本阶段只确认了核心流和字段痕迹，未取得段落级布局信息。

## 多篇文献与边界

一个卷通常包含多篇独立文献，但第 5-7 卷是大型专著，边界层级不同。

可见组织方式：

- 第 1-4 卷：多篇文章、书摘、演说、序言、专题文本，按编年或主题排列。
- 第 5-7 卷：《资本论》三卷，包含序言、篇、章、节等层级。
- 第 8 卷：《资本论》手稿选编，包含多份手稿摘选。
- 第 9 卷：两大文献《反杜林论》《自然辩证法》及内部章节。
- 第 10 卷：大量书信，按年份和编号组织。

后续识别文章标题和边界时建议优先组合使用：

- 目录项中的标题和页码范围；
- 正文中标题候选段落；
- 作者行；
- 分节或分页信息；
- 字体、字号、居中等格式特征；
- 标题前后的空段、插图说明、注释和索引区域。

## 风险清单

- 目录名叫 `raw_docx`，但 90% 文件不是 DOCX。
- 标题样式不统一，样式覆盖率不足。
- 页码信息主要是排版字段，不是正文显式元数据。
- 目录页码可以给文献边界候选，但不能定位段落页码。
- 第 1 卷的分节结构很有价值，但必须先验证，不能直接视为原书页码。
- 第 2-10 卷需要 Word 二进制解析、受控转换或排版引擎辅助，否则无法可靠获得段落页码。

## 下一阶段建议

正式开发搜索前，应先做“结构化抽取原型”，目标不是搜索，而是产出可审计中间数据：

1. 为每卷生成 `source_file` 元数据，记录格式、大小、哈希、卷次。
2. 为第 1 卷验证分节是否等于原书分页。
3. 为第 2-10 卷选择可靠解析路线：Word/LibreOffice 转换、专门 `.doc` 解析库，或回到更可靠的 PDF/扫描源。
4. 从目录抽取初始文献清单和页码范围。
5. 建立人工抽样校验表，校验文章起始页、标题边界和页码映射。
