# 下一批功能规划

日期：2026-07-18
状态：B（期刊链路核心）已实施 2026-07-19；A（目录扫描）已实施 2026-07-19；D1（批量识别）已实施 2026-07-19。
剩余：C（多解析 API）、D2（--export-json 可选）、D3（数据备份/迁移）。

## A. 文献目录扫描

设置页新增"文献目录"（可配置一个或多个本机文件夹，存 preferences）。导入页新增"扫描目录"：

- 列出目录（含子目录）中所有 PDF/DOCX，与已导入文献比对（文件哈希优先、文件名兜底），分为"新文件 / 已导入 / 同名不同内容"三类；
- 勾选批量导入，逐个走现有导入管线（复制进应用语料目录，**原件永不移动或删除**）；
- 非原生文本的 PDF 在列表中标注"需 OCR"，**不自动提交 MinerU**（消耗配额），由用户勾选确认后才提交；
- 复用现有导入队列的动态步骤条显示批量进度。

风险低，体验提升明显。

## B. 书籍 / 期刊文献区分

引文层已完成：`citations.py` 的 `journal_article` 分支输出
`张义修：《当代德国语境中的马克思哲学与批判理论——访拉尔·耶吉教授》，《哲学动态》2017年第2期，第101页。`（中文）与
`郑作彧. 化用的生活形式，还是共鸣的世界关系？——批判理论第四代的共识与分歧[J]. 社会科学, 2021(3):53.`（GB/T），
且强制引用命中页而非文章页码范围（`test_journal_citation_uses_hit_page_not_article_range`）。

待补链路：

1. **类型识别与切换**：导入时按期刊特征（DOI、卷期号样式、页眉刊名、页码范围小）给出 `journal_article` 建议；抽屉书目信息里允许人工切换 书籍/译著/期刊论文；
2. **字段集按类型切换**：期刊显示 作者/篇名/刊名/年份/期号/起止页码；书籍保持现有字段；缺失字段标记同步按类型调整（期刊不要求出版社/出版地）；
3. **文献库类型筛选**：与格式（Word/PDF）、语言（中文/外文）并列增加"书籍/期刊"筛选；
4. 期刊 PDF 页码映射通常是"PDF 第 1 页 = 文章起始页"，自动映射对短文档给出单段 offset 建议即可。

用户近期要导入期刊论文，优先级最高。

## C. 多解析 API（千问等）与自定义站点

现状：MinerU 的"API 地址"已可配置（`mineru_api.py::api_base`，设置页有输入框）——**中转站点已支持**。默认与推荐保持 MinerU。

新增设计：

1. 设置页"解析服务"节：服务商列表，MinerU（默认，推荐标记）+ 可添加 OpenAI 兼容接口的视觉模型（如通义千问 qwen-vl，DashScope 或中转 base URL + key + model 名）；
2. 解析抽象层：`parse_pdf_with_mineru` 泛化为 `parse_pdf_with_provider`，千问路径为逐页渲染图片 → VL 识别文本 → 拼装为与 MinerU 兼容的页文本结构；
3. 注意：VL 输出没有 MinerU 的版面/页码结构信息，自动页码映射的 `mineru_candidates` 通道对该来源为空，靠页面边缘文本通道兜底；界面上如实标注解析来源；
4. 密钥仍只存本机（`%LOCALAPPDATA%\MEFinder`），与 MinerU 同等待遇。

工作量最大、失败模式最多，建议排在 A/B 之后。

## D. 顺带建议（小而值）

1. **批量重新识别书目信息**：最近几轮识别改进（末页版权页扫描、CIP 覆盖旧值、© 年份、ISBN 双行窗口）对存量书大多有效；加"一键重新识别所有缺字段文献"批量任务，一次清掉黄色警告。
2. **`--export-json` 改为可选**（§7 唯一剩项）：`data/index.json` 300MB 仅是备份，默认不再随每次重建全量重写。
3. **数据备份/迁移**：把 `%LOCALAPPDATA%\MEFinder\runtime` 打包导出/导入，换电脑不丢页码校准与元数据。

## 建议顺序

B（期刊链路，引文层已就绪）→ A（目录扫描）→ D1（批量识别）→ C（多 API）→ D2/D3。
