# 下一批功能规划

日期：2026-07-18
更新：2026-07-26
状态：A（目录扫描）、B（期刊链路）、C（多解析 API MVP）、D1（批量识别）、D2（可选 JSON 导出）、D3（数据备份/迁移）均已实施。

## A. 文献目录扫描

设置页新增"文献目录"（可配置一个或多个本机文件夹，存 preferences）。导入页新增"扫描目录"：

- 列出目录（含子目录）中所有 PDF/DOCX，与已导入文献比对（文件哈希优先、文件名兜底），分为"新文件 / 已导入 / 同名不同内容"三类；
- 勾选批量导入，逐个走现有导入管线（复制进应用语料目录，**原件永不移动或删除**）；
- 非原生文本的 PDF 在列表中标注"需 OCR"，**不自动提交 MinerU**（消耗配额），由用户勾选确认后才提交；
- 复用现有导入队列的动态步骤条显示批量进度。

风险低，体验提升明显。

## B. 书籍 / 期刊文献区分

引文层已完成：`citations.py` 的 `journal_article` 分支输出
`张义修：《当代德国语境中的马克思哲学与批判理论——访拉尔·耶吉教授》，《哲学动态》2017年第2期，第101页。`（中文）与
`郑作彧. 化用的生活形式，还是共鸣的世界关系？——批判理论第四代的共识与分歧[J]. 社会科学, 2021(3):53.`（GB/T），
且强制引用命中页而非文章页码范围（`test_journal_citation_uses_hit_page_not_article_range`）。

已完成链路：

1. **类型识别与切换**：导入时按期刊特征（DOI、卷期号样式、页眉刊名、页码范围小）给出 `journal_article` 建议；抽屉书目信息里允许人工切换 书籍/译著/期刊论文；
2. **字段集按类型切换**：期刊显示 作者/篇名/刊名/年份/期号/起止页码；书籍保持现有字段；缺失字段标记同步按类型调整（期刊不要求出版社/出版地）；
3. **文献库类型筛选**：与格式（Word/PDF）、语言（中文/外文）并列增加"书籍/期刊"筛选；
4. 期刊 PDF 页码映射通常是"PDF 第 1 页 = 文章起始页"，自动映射对短文档给出单段 offset 建议即可。

用户近期要导入期刊论文，优先级最高。

## C. 多解析 API（千问等）与自定义站点

实施状态：**MVP 已完成**。MinerU 保持独立的免费默认服务；其下新增“其他解析 API”，支持保存多套 OpenAI 兼容视觉模型或中转接口。

已实施：

1. MinerU 设置区和现有解析链路保持不变，并标注为免费默认服务；
2. 可添加多套 OpenAI 兼容视觉接口，每套配置服务名称、base URL、API Key、模型名和启用状态；密钥摘要永不回显；
   填写地址和 Key 后会尝试通过 `/models` 自动读取并排序模型，使用可搜索选择框，同时保留手动输入；
3. `parse_pdf_with_provider` 将 PDF 逐页渲染为图片，调用视觉模型识别，再生成通用结构化清单并复用现有索引、搜索与页码校准链路；
4. 导入页可明确选择 MinerU 或已配置的其他视觉 API；
5. MinerU 失败时默认停止并提示用户手动改用备用接口；设置页可显式开启“自动切换”，开启前提示可能产生费用；
6. 失败任务可以直接在导入队列中改用默认备用接口重试，无需再次上传 PDF；
7. 密钥仍只存本机：源码/便携版位于 `config/vision_api.local.json`，安装版位于 `%LOCALAPPDATA%\MEFinder\vision_api.local.json`。

已知限制：通用 VL 输出不具备 MinerU 的完整版面框信息，自动页码映射主要依赖页面边缘文本通道；不同中转服务对多模态消息的兼容程度仍需用实际模型逐一验证。

## D. 已实施的小功能

1. **批量重新识别书目信息（D1，已实施）**：对缺字段文献执行一键重新识别，复用末页版权页扫描、CIP 覆盖旧值、© 年份、ISBN 双行窗口等识别改进。
2. **`--export-json` 改为可选（D2，已实施）**：SQLite 作为权威索引，默认不再重写约 300MB 的 `data/index.json`；仅在显式传入 `--export-json` 时导出。
3. **数据备份/迁移（D3，已实施）**：设置页可导出与恢复页码校准、书目元数据、MinerU 页偏移清单及偏好设置；恢复后自动重建索引。

## 实施结论

B（期刊链路）→ A（目录扫描）→ D1（批量识别）→ D2（可选 JSON 导出）→ D3（数据备份/迁移）→ C（多解析 API MVP）已经完成。
