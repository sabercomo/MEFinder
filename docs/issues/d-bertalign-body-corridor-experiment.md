# D：Bertalign 两步法实验

状态：2026-09-06 已完成初始真机实验及四条原书抽查；不进入 0.5.2 正式 DP。结果见 `reports/d-bertalign-experiment-2026-09-06.md`。

方法依据：[Bertalign 原作者仓库](https://github.com/bfsujason/bertalign) 与 [Liu & Zhu 论文](https://doi.org/10.1093/llc/fqac089)。先用 1:1 对齐建立近似锚点，再沿受约束路径做多对多恢复；不能把简单放宽当前 DP 的最大块长称作复现 Bertalign。

实验约束：

1. 输入为固定分段集、固定模型缓存及已复核正文区域；数据库只读，结果单独输出。
2. 恢复范围取相邻可靠锚点间的双侧正文开区间，不含锚点本身；无双侧边界的首尾区域不恢复，不跨区域或跨锚点。
3. 副文本与集中尾注禁止进入候选；不能仅以“当前没有 accepted 链接”判断可恢复。
4. 同一缓存分别评估当前 DP、两阶段恢复；记录完整成员集合与分数、耗时、峰值内存、覆盖冲突及人工/AI 原书判定来源。
5. 正确与部分对应分开计分，不能用 unmatched 降幅代替准确率。37 条现有复核只作定向回归集，另抽正文新增恢复候选核对原书。

已完成：20 个固定分段集正文边界复核；缓存求和原型对照；固定上游 corelib 两阶段 DP + 真实拼接文本 E5 嵌入；一个结构走廊 226 条链接，唯一覆盖旧 unmatched 的候选核对为正文正确。

后续门禁：扩展跨书正文走廊样本，评估覆盖冲突、准确率与耗时后再决定是否接入正式 DP。未编号标题及重复 Notes 的新书自动识别仍有局限，不得把本批复核边界当通用规则。脚注/尾注覆盖冲突另见 `note-layout-alignment-conflicts.md`。

新增阻断证据：全库复核发现 FR→ZH 的 `term:l-age-d-or` 同词异义锚点，以及 JA→ZH 的跨章 `number:18` 锚点。旧 MiniLM 和新 E5 均存在，不能把“当前已有锚点”视为“可靠锚点”。扩大 D 前须在实验路径验证锚点筛选；本轮未擅改正式锚点算法。详见 `reports/e5-full-rerun-2026-09-06.md`。

## 2026-09-08：最终锚点候选收口与粗定位试用

事实：接手时 `semantic_alignment._validate_soft_anchors` 已调用正式叶子模块的筛选，版本仍是
21 / 19。本轮补为 22 / 20，并保留 v21 结果读取与配方恢复兼容；旧 run 不参与新生成复用。
正式入口在独立副本上重算 16 个书对，完整链接成员、接受状态、分数及标题锚点与最终候选全部一致。
证据见 `reports/alignment-v22-trial-validation-2026-09-08.md` 及同名 JSON。

范围：本次仅验证既有候选进入实际阅读流程，未接入更大的 Bertalign 两阶段恢复、翻译模型或新阈值。
`reports/d-anchor-acceptance-2026-09-07.md` 中的粗定位验收结论继续有效；四条争议样本保持未决，
不能将实现一致性视为新增准确率证明。下一步由用户在阅读器判断附近论述是否足够定位，按具体错例反馈。

## 2026-09-14：二轮（路径×表示双轴消融）——决定性阴性，路线收口

背景：用户授权重启（token 成本不再是约束），要求以当前正式算法为基线回答"错误来自搜索路径还是分组表示与评分"。材料清单、基线与四臂设计见 `reports/d-two-stage-round2-2026-09-14.md`（驱动 `scripts/d_two_stage_{baseline,corridors,evaluate}.py` + 重建的 `scripts/bertalign_corridor_experiment.py`，机器结果同目录 JSON）。

事实：
- 17 对书对用当前正式代码全量再生，raw-DP 层经生产调用链 17/17 精确复现（旧方法从落库链接反推分区边界会被注释链替换污染——本轮的方法论修正）。
- 四臂（A 基线 / B +1:4/4:1 / D4 拼接重嵌入 / C 双变）在 53 条选中走廊上：严格修复 0/9 可行动夹具；控制回归 B 5 条、C 16、D4 15（门禁 <2%）；n74 三臂全部降级（B 换成员、C/D4 掉置信度）；apparatus/噪声违规 182–231 条（门禁零）。A'==A 自检 53/53。
- 拼接表示机制性抬高噪声/标签段分数（连缀稀释）；扩转移不产生严格修复只换近似并列赢家。

结论：两阶段恢复路线到此为止（四选一之 3）。残留错配 = 分段粒度固有近似并列 + 结构性出域 + 分段不对称，均为正交病因。后续杠杆：正文区检测塌缩修复（7 条控制样本出域的直接原因）、分段层脚注切分、n93 阈值边缘的产品决策。09-06 首轮报告与 note-channel 原始 JSON 已不可恢复（旧机器），本轮判定全部建立在 v13/v22 坐标系与已提交 JSON 上。
