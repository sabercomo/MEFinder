# OCR 乱码源段被当正文配对

状态：独立待办，解析/导入质量问题，不属对齐算法或段质量门范围。优先级低于 D（正文走廊两步恢复）。

依据：`reports/note-channel-accuracy-2026-09-06.md` 的 100 条全库随机复核。修完锚点门 + 段质量门 + 注释通道后，12 条错配里有 **3 条源自目标 PDF 该段 OCR 严重损坏、无可配对文本**，却仍以高分进入 accepted 正文链接：

- n24：英文目标段 `very! / hing wJlich arises... aCllvity oǸ` ↔ 中文“真实的良心…宗教的良心”。
- n28：英文目标段纯乱码 `Ð! / ification is ˽eXfÑiIictude˼… / tio;` ↔ 德文导言正文。
- n80：英文目标段 `sality within itseJf… / For indeteʚe ɵ eing no stinction…` ↔ 德文“纯思维/人格”。

样本与 run/order_index 见 `reports/note-channel-random-sample-2026-09-06.json`（错配编号含 24、28、80）。

范围：识别 OCR 严重损坏的段（高比例非常规字符 / 控制字符 / 无法构成词的字母串），在**导入/解析层**标注或排除出正文配对通道，而不是在冻结的对齐 DP 或段质量门里补救。段质量门只处理“信息量过低”的噪声（纯符号、单字符），不负责判定“文字存在但被 OCR 破坏”。

验收：临时库复现上述 3 段；损坏段可被标注并排除出正文 accepted；不误伤正常短段与含少量 OCR 误差的正常段；重抽样中该类错配归零。具体判据（字符类别比例阈值等）待定，需原书抽查校准，避免过度排除。

备注：这三段的英文侧是同一批黑格尔英文 PDF（`pdf-import-0cdfc70dc7574b98` 等），运行语言元数据一度为 `und`，OCR 质量偏差可能集中在该导入源，值得先核对该文件的解析质量再决定是重导入还是加标注。
