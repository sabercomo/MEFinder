# OCR 乱码段诱发的 DP 走廊错配

状态：从属 D（正文走廊两步恢复）。**核对后修正定性**：不是“无可配对文本 / 解析层独立问题”，
而是 OCR 乱码**诱发**了锚点间正文走廊内的 DP 接错——正确译文都在邻近 segment，属 D 射程内。
另有一个次要、可选的解析质量清理面。

依据：`reports/note-channel-accuracy-2026-09-06.md` 的 100 条复核 + 逐条回原页核对。3 条曾被
误判为“纯 OCR 乱码无可配对”，实为 DP 接错段：

- n24（order 3007）：当前接 T3553–3555（§138 附释，OCR 花），**正确英文在 T3535–3536**。
- n28（order 1469）：当前乱码成员 T1198–1200，**正确英文在 T1193**。
- n80（order 1729）：当前 T1861–1863（§34 前段，OCR 花），**正确英文在 T1879–1880**。

英文 PDF 原页正文实际可读（`dist/MEFinderData/runtime/corpus/raw_pdf/Elements of the
Philosophy of Right …z-library… .pdf`）；乱码只出现在被 DP 错接到的那几段，正确目标段清晰。

范围（两条线，主次分明）：
1. **主：交 D 修对齐**。这 3 条与其余 8 条 DP 偏移同源，D 的走廊二次对齐应把它们重接到上列
   正确 order_index（已写入 D 金标夹具）。
2. **次（可选）：解析质量清理**。被错接的乱码段（如 T3553–3555 / T1198–1200 / T1861–1863）
   本身是 OCR 损坏产物；识别并标注/降权这类段，可能帮 DP 少被引偏，是 D 的补充而非替代。
   判据（非常规字符比例等）待定，须原书抽查校准，避免误伤含少量 OCR 误差的正常段。

验收：D 重接后上述 3 条命中正确目标；若另做解析清理，临时库复现、不误伤正常短段、重抽样
中该类错配归零。

备注：这批英文侧集中在同一黑格尔英文 PDF（`pdf-import-0cdfc70dc7574b98` 等，语言元数据一度
`und`），OCR 偏差或集中于该导入源，值得先核该文件解析质量。
