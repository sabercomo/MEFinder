# 编号／节号标题检测与配对缺口（issue #17）

2026-09-07：已定位并修复两个互相独立的通用缺口：PDF 跑眉清理误删正文 `§N` 标记；对齐正文切片
使标题检测失去目录上下文，且中途注释提前终止后续章节扫描。完整诊断、负面结果与 17 个真实 run
复核见 `reports/issue-17-heading-anchor-validation-2026-09-07.md`。

事实：

- R9 中文六章都在文本层，不是扫描图片；形态包括无编号标题以及与正文并段的 `02`–`06` 标题。
- n93 德文 `§202` 在原 PDF 与页文本中存在，是重复跑眉归一误删；英文正文 `§203` 可见但不在 PDF
  原生文字层中，本修复没有伪造它。
- 尾注区的第二个英文 `§203` 仍被区域门排除。
- v21 副本重跑后 R9 检出 chapter:1–6，unmatched 3913→170；n93 的源 `§203` 候选进入正确
  §202↔§204 走廊，但 0.829968 低于 0.83，按既有门槛降级而非错误自动定位。

推断：英文 PDF 若未来由解析层补出正文 `§203`，现有通用段号锚点逻辑应能直接形成
`paragraph:203`；本 issue 不扩张为 OCR 修复。
