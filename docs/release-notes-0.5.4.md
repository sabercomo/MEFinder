# MEFinder 0.5.4

2026-09-11：独立阅读窗口入口置顶，译本对照默认选择跨语言版本，检索详情去除外框；macOS arm64 测试包已构建，等待用户实际验收，未正式发布。独立阅读窗口的关闭回执死锁已修复，macOS 隐藏原生窗口及自然退出回归通过。

## 更新内容

- 独立阅读窗口开关移至“设置 → PDF 阅读”最上方的“阅读窗口”区域；开关仍默认关闭，开启后下次打开阅读时生效。
- 译本对照重新打开时在跨语言候选中恢复选择，避免中文 PDF 自动对到上次选过的中文 EPUB；不固定优先英文，简繁中文视作同语言，当前阅读中的手动切换保持。

- 检索详情去掉外层圆角卡片与四周留白，宽屏和窄屏均直接铺满内容区；保留正文间距、独立滚动和固定操作栏。

- 设置 → PDF 阅读增加“独立阅读窗口”，默认关闭；开启后结构化阅读和译本对照在原生窗口中打开。
- 每次打开可保留独立的文献、阅读位置和对照状态，主窗口继续搜索。
- 阅读窗口可移动、缩放和关闭；主窗口退出时统一关闭阅读窗口并释放后台。
- 开关从下一次打开阅读时生效，关闭时保留原来的结构化阅读弹层；PDF 原文打开方式保持独立。
- 阅读内容与窗口容器通过小型宿主接口分离，复用原有数据、定位、引文和译本对照逻辑。
- 关闭采用单向窗口状态通知，避免窗口销毁后等待 JS 回执导致进程残留；后台正常收尾，不使用固定延迟销毁或强制退出。

- 新增可重复性能基准：固定合成语料、普通搜索、Markdown/EPUB 导出及真实 MiniLM 对齐期间的搜索；保存 HTTP 失败率、定位一致性、RSS 和后端启动/退出数据，支持同配置前后比较。

## 验证与边界

本轮全量 unittest 2101 项通过（22 项条件跳过，含需显式启用的原生测试；94.310 秒），Ruff F 与前端守卫通过。新增 7 项测量机制/夹具测试，不以机器耗时作为 CI 硬门槛。
macOS 原生测试单独启用通过：默认弹层、独立锚点与对照状态、移动缩放、正文填满窗口、关闭后重开及整个进程自然退出。
修复场景连续 3 次自然退出；旧关闭 API 在隔离对照实验中仍复现回执线程卡死。
此前另一例无栈转圈的原因尚不能确认相同；可见交互最终验收、Windows 原生 GUI 与两平台成品构建仍属后续发布门禁。
详细证据及限制见 `reports/reader-window-validation-2026-09-10.md`。
新增偏好和窗口契约见 `docs/contracts/v0.5.4-reader-window.md`。

性能测量方法见 `docs/performance-baseline.md`，实测与局限见 `reports/performance-baseline-v0.5.4-2026-09-10.md`。

## 2026-09-11 macOS arm64 测试包

- 全量 unittest 2103 项通过（22 项条件跳过）；构建流程再次运行全部测试通过，Ruff F、前端守卫、Node 语法检查通过。
- 新增 `test_search_detail_is_flush_without_outer_card_frame`，先复现外层卡片留白，再验证各断点去框；同步集成分支装配指纹。
- 官方 `build_macos.sh` 完成主应用、MCP STDIO 冒烟、严格签名、ZIP 解包、DMG 挂载及复制后签名校验；独立 SHA-256 复核通过。
- 按用户要求未使用 computer use，未进行本轮 GUI 验收；Windows 0.5.4 包未构建，未创建 tag 或正式 Release。

| 产物 | 字节数 | SHA-256 |
|---|---:|---|
| `MEFinder-v0.5.4-macos-arm64.dmg` | 176210949 | `1ca6f3025047d63ca18528e84584bfee7b11c193272bf5b21a7e9351342ed1b2` |
| `MEFinder-v0.5.4-macos-arm64.zip` | 166172464 | `2e9a8840dd3e3459b56329945bbf6f38a08111cf8a3f2cbbb7fcbeab3e2660e7` |

## 2026-09-11 第二次测试包修订

核实前一个 0.5.4 包及 `/Applications/MEFinder.app` 已含独立窗口功能，`codex/v0.5.4` 无未集成提交；
当时开关未设置，按默认关闭处理。用户报告看不到开关，尚无本轮 GUI 证据确认原因；
本次将该开关移至面板顶部。未擅自修改用户偏好，未使用 computer use。

增加跨语言默认选择的真实 JS 测试，覆盖同语言旧记忆、跨语言记忆、简繁、德中、会话手选及无跨语言候选；
服务端直接读取已保存 segment set 的语言，避免读不到源语言而退回错误版本。

本修订完整构建：2105 项 unittest 通过（22 skip）、Ruff F、Node、严格签名、MCP STDIO 冒烟、ZIP/DMG 校验通过。包内模板与阅读脚本逐字节匹配源码。以下新摘要取代前一次同名本地测试包：

| 产物 | 字节数 | SHA-256 |
|---|---:|---|
| `MEFinder-v0.5.4-macos-arm64.dmg` | 176212061 | `c79646d82ae7087193728cc4b9eb3c3ded43cbaf5c43df1cb7eb6c1ed25fc6b7` |
| `MEFinder-v0.5.4-macos-arm64.zip` | 166172740 | `6eaccd6c626fb9a16de43d9e400ebfbf2c796ed85ee8bc37fd654f4db4bca3c0` |
