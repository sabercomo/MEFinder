# 文献原句定位器 v0.5.5（迭代中）

> 2026-09-15：**迭代中,未发布**。0.5.5 是「对齐计算组件化」版本——把译本对齐所需的数值计算栈(NumPy / ONNX Runtime / fastembed,约 94 MiB)从主程序里移出,改为按需安装的独立运行时。本轮已落地**主包精简闭环**:桌面主包定向构建由 206.4 MiB 降到 100.8 MiB(macOS ARM 实测,不含 MCP sidecar),不再携带数值栈,计算改由独立解释器运行。**尚未完成**(见文末):设置页的安装/升级/卸载按钮界面、Windows / macOS Intel 冻结验证、sidecar 精简。当前结论:后端与打包边界已就绪,面向用户的安装入口 UI 为下一步。

## 更新内容

### 译本对齐改为按需组件,主程序更小

- 桌面主程序不再打包对齐计算数值栈;安装包体积显著下降(macOS ARM 定向构建实测约减半)。检索、阅读、题录、已有对齐结果与引用定位不受影响,均不依赖该栈。
- 译本对齐的嵌入计算与模型探针改在**独立解释器**中运行;计算源随应用交付,由独立运行时的解释器加载。
- 未安装独立运行时时,译本对齐会**明确显示不可用原因**(而非静默失败或误报);已生成的对照与人工修正保留可用。

## Bug 修复

- 精简主包下触发模型下载时,若未安装独立运行时,给出明确的「运行时未安装」提示,替代原先会抛出的底层 `ImportError`。
- 更正 v0.5.4 体积盘点报告中「numpy 全部函数内 lazy import」的表述:实为 3 处 `TYPE_CHECKING` 守卫 + 1 处零引用的模块级导入(详见 `docs/issues/note-alignment-compute-runtime-2c.md`)。

## 尚未完成（不声称已交付）

- 设置页「译本对齐」安装 / 升级 / 卸载**按钮界面**(端点已就位,镜像本地 OCR 组件)。
- Windows / macOS Intel 冻结产物冒烟;完整 `build_macos.sh` 发布级 ZIP/DMG/签名。
- 真机 uv 安装独立运行时后的正向计算逐位一致性核验。
- MCP sidecar 数值栈精简(另立主题)。
