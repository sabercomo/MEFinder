"""Vendored third-party source, kept isolated from MEFinder's own modules.

Each subpackage carries its upstream license and a ``MODIFICATIONS.md`` that
itemizes every change made relative to the pinned upstream commit, so the
source can be updated later against a clear boundary.
"""
